Tech Stack: Angular, .NET Core, and SQL Server
Details:
The project is a Pharmacy Management System developed using .NET Core for the backend and Angular for the frontend.
To run the application, build the .NET project first (credentials are configured in appsettings.json), and then use the command ng serve --o to start the Angular frontend.
